/// Represents special types or roles that a tile or entity can have in the simulation.
/// Used as a common qualifier for things like targets, origins, or tile types.
public enum UniversalQualifierMarker
{
    Queen,
    Resource,
    DefaultTile,
    Blocker
}